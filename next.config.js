/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost']
  },
  /*
  images: {
    path: "/",
  }
  */
}
module.exports = nextConfig
